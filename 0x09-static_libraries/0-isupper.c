#include "main.h"
/**
 * _isupper - function checks for uppercase alphabets
 * @c: Inputed alphabet
 * Return: return 1 if true or 0 if false
 */
int _isupper(int c)
{
if (c >= 65 && c <= 90)
return (1);
else
return (0);
}
